function cambiaMensaje() {
  document.getElementById("loading-message").innerText = "Solo para mi luisa🟣!";
}

setTimeout(cambiaMensaje, 2300);

setTimeout(function () {
  alert("Recibiste un abrazo a la lejania de parte de sebastian");
}, 2400);
